package.Eks-node-monitoring-agent = {
    interfaces = (1.0);

    deploy = {
        map = (default, "-gopath/**");
    };

    build-system = make;

    build-tools = {
        1.0 = {
        };
    };

    dependencies = {
        1.0 = {
        };
    };

    runtime-dependencies = {
        1.0 = {
        };
    };

};
